<div class="notification">
    <section class="section is-mobile">
        <div class="container">
            <div class="columns">
                <% if user_signed_in?%>        
                    <%= render 'feed' %>
                <% else %>
                     <%= render template: 'home/index' %>
                <% end %>      
            </div>
        </div>
    </section>
</div>


