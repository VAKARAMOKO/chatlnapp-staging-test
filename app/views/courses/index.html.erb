    <section class="section">
        <div class="container">
            <div class="columns is-centered">
                <div class="column  is-10">
                    <% if user_signed_in?%>        
                    <%= render 'feed' %>
                <% else %>
                     <%= render template: 'home/index' %>
                <% end %> 
                </div>     
            </div>
        </div>
    </section>

